# 28.5: Complete the admission-aware release gate

**What to build:** Revalidate the UAT route set after admission is integrated and establish the complete default-off production release gate. OpenTelemetry remains owned by deferred Ticket 24, and Ticket 29 owns load, parity, and default production enablement.

**Blocked by:** 27: Integrate admission with the Run lifecycle; 28: Enable the v2 route set for UAT functional testing.

**Status:** ready-for-agent

- [ ] Query and resume start running directly or fail fast; neither path queues, and both enforce the admission contract before changing authoritative state.
- [ ] A release-gate integration suite covers Tenant isolation, persist-before-deliver, history, replay/live, resume, cancel, and fail-fast admission.
- [ ] Capacity rejection returns `429` with `Retry-After` and does not create or mutate Conversation, Message, slot, Run, claim, Event, epoch, or checkpoint state.
- [ ] The v2 admission path is independent of the legacy middleware that only matches `/api/`; v1 routes and engine remain unchanged.
- [ ] The complete v2 route set remains default-off after this ticket; Ticket 29 owns default production enablement.
